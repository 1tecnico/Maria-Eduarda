<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Jogo de STOP</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #0f172a;
      color: #fff;
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 20px;
      margin: 0;
    }

    h1 {
      color: #38bdf8;
      margin-bottom: 10px;
    }

    #letra {
      font-size: 3em;
      margin: 20px 0;
      color: #facc15;
    }

    .btn {
      padding: 12px 25px;
      background: #38bdf8;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      margin: 10px;
      font-size: 1em;
      color: #000;
      font-weight: bold;
      transition: 0.2s;
    }

    .btn:hover {
      background: #0ea5e9;
      transform: scale(1.05);
    }

    table {
      margin-top: 20px;
      border-collapse: collapse;
      width: 95%;
      max-width: 600px;
    }

    th, td {
      border: 1px solid #38bdf8;
      padding: 10px;
      text-align: center;
    }

    th {
      background: #1e293b;
    }

    input {
      width: 95%;
      padding: 8px;
      border-radius: 5px;
      border: none;
      outline: none;
      text-align: center;
      font-size: 1em;
    }

    footer {
      margin-top: 30px;
      font-size: 0.9em;
      color: #94a3b8;
    }
  </style>
</head>
<body>
  <h1>🎲 Jogo de STOP</h1>
  <button class="btn" onclick="gerarLetra()">🎯 Sortear Letra</button>
  <div id="letra">?</div>

  <table>
    <tr>
      <th>Categoria</th>
      <th>Resposta</th>
    </tr>
    <tr>
      <td>Nome</td>
      <td><input type="text" id="nome"></td>
    </tr>
    <tr>
      <td>Animal</td>
      <td><input type="text" id="animal"></td>
    </tr>
    <tr>
      <td>Fruta</td>
      <td><input type="text" id="fruta"></td>
    </tr>
    <tr>
      <td>Cidade</td>
      <td><input type="text" id="cidade"></td>
    </tr>
    <tr>
      <td>Objeto</td>
      <td><input type="text" id="objeto"></td>
    </tr>
  </table>

  <button class="btn" onclick="finalizar()">✅ Finalizar</button>

  <footer>
    Feito com ❤️ para brincar no GitHub Pages
  </footer>

  <script>
    function gerarLetra() {
      const letras = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
      const letra = letras[Math.floor(Math.random() * letras.length)];
      document.getElementById("letra").textContent = letra;
    }

    function finalizar() {
      const nome = document.getElementById("nome").value;
      const animal = document.getElementById("animal").value;
      const fruta = document.getElementById("fruta").value;
      const cidade = document.getElementById("cidade").value;
      const objeto = document.getElementById("objeto").value;

      al
